name: Test D — reusable stub

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would deploy ${{ inputs.app_name }}"
